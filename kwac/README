kwac - k webassembly compiler

s is a k program (k7;i) that emits webassembly byte code.
the resulting program is (not yet) a k interpreter itself (a subset of k7).

i(../k.go) is a reference implementation that uses the same memory layout as the wasm version (only on little endian).

Wasm reference
 webassembly.github.io/wabt/demo/wat2wasm (interactive)
 sunfishcode/wasm-reference-manual/blob/master/WebAssembly.md
 