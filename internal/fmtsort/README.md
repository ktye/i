This is a verbatim copy from the internal packge of the standard library.
It is used to keep a consistent order for dict keys (not just for printing).

This is incompatible to other K implementations.
