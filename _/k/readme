# k
all:
	# install go-1.13
	cp ../../k.go .
	echo -e "module github.com/ktye/i/_/k\n\ngo 1.13" > go.mod
	GOPATH="" GO111MODULE=on go build -ldflags="-s -w"
	# there should now be an executable k(.exe) ~5MB
