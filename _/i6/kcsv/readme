$kcsv [fmt] f.csv [files.k] [-e expr]
 stores csv as inverted table(L of columns) to x:..
 eval expr, or drop into k

$kcsv [fmt] f.csv -t|-b
 -t writes inverted table in k-text format to stdout
 -b                          binary

$ksv [fmt] f.csv -h
 print selected colnames from first line

fmt
 ;0i4i             ;-separated, column 0 and 4 as int
 ,2hifs            skip 2 header lines

i(int) f(float) s(symbol) c(chars) z(2 cols: abs,ang)
default sep (1.row max# of ,;|t space:strings.Fields)
