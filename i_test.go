package i

/*
type myComplex complex128
type myFloat float64
type myShort int16
type myBool bool

func TestV(t *testing.T) {
	f1, f2 := 1.1, 2.2
	c1, c2 := complex(1, 2), complex(3, 4)
	c3, c4 := myComplex(complex(5, 6)), myComplex(complex(7, 8))
	b1, b2 := myBool(true), true
	testCases := []struct {
		a, b interface{}
	}{
		{f1, f2},
		{c1, c2},
		{c3, c4},
		{f1, c4},
		{c1, f2},
		{b1, b2},
	}

	for _, tc := range testCases {
		r := add(tc.a, tc.b)
		fmt.Println(tc.a, "+", tc.b, "=", r)
	}
}
*/
